// Function as Expression in JavaScript

// Defining a function as an expression and assigning it to a variable
const add = function(a, b) {
    return a + b; // Returns the sum of a and b
};

// Logging the function reference (not executing it)
console.log(add);

// Calling the function with arguments and logging the result
console.log(add(25, 78));
